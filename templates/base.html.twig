<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}La Cuisine De Caro{% endblock %}</title>

    {# Polices Google #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Courier+Prime&family=Lato:wght@400;700&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">

    {# Font Awesome pour les icônes #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    {# C'EST ICI QUE CA SE JOUE : Les tags Encore #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>

{# 1. HEADER (Prend 100% de la largeur) #}
{% include 'partials/_header.html.twig' %}

{# 2. HERO IMAGE (Conditionnelle : Pas sur l'admin) #}
{% if app.request.get('_route') != 'app_admin' and app.request.get('_route') != 'app_home' %}    <div class="hero-section">
        <div class="hero-content">
            {# Tu pourras changer ce titre dynamiquement plus tard si tu veux #}
            <h2>
                {% if app.request.get('_route') == 'app_order' %}
                    Réservez votre Moment
                {% elseif app.request.get('_route') == 'app_blog' %}
                    Les Actus de Caro
                {% else %}
                    L'Excellence à votre Table
                {% endif %}
            </h2>
        </div>
    </div>
{% endif %}

{# 3. CONTENU PRINCIPAL (Avec la logique pour la nappe) #}
{# On définit la classe de base #}
{% set mainClasses = 'container' %}

{# Si on est sur la page de commande, on ajoute la classe 'page-with-nappe' #}
{% if app.request.get('_route') == 'app_order' %}
    {% set mainClasses = mainClasses ~ ' page-with-nappe' %}
{% endif %}

<main class="{{ mainClasses }}">
    {% block body %}{% endblock %}
</main>

{# 4. FOOTER (Prend 100% de la largeur) #}
{% include 'partials/_footer.html.twig' %}
</body>
</html>
