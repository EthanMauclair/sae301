<header class="site-header">
    <div class="header-inner">
        <div class="brand-logo">
            <a href="{{ path('app_home') }}">
                <span class="icon"></span> La Cuisine <span class="highlight"> de Caro</span>
            </a>
        </div>

        <nav class="main-nav">
            <ul>
                {# Note: Remplace 'app_home', 'app_order', etc. par les vrais noms de tes routes #}
                <li>
                    <a href="{{ path('app_home') }}" class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}">Accueil</a>
                </li>
                <li>
                    <a href="{{ path('app_order') }}" class="nav-link {{ app.request.get('_route') == 'app_order' ? 'active' : '' }}">Commande & Panier</a>
                </li>
                <li>
                    <a href="{{ path('app_blog') }}" class="nav-link {{ app.request.get('_route') == 'app_blog' ? 'active' : '' }}">Blog & Actus</a>
                </li>
                <li>
                    <a href="{{ path('app_admin') }}" class="nav-link admin-link">Back Office</a>
                </li>
            </ul>
        </nav>
        <div class="header-decoration"></div>
    </div>
</header>
